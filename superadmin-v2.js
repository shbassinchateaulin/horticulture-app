(()=>{const K='horticulture-admin-users-v2';const ALL=['communication','sorties','adherents','comptabilite','suggestions','acces'];const LABELS={communication:'Actualités',sorties:'Sorties',adherents:'Adhérents',comptabilite:'Comptabilité',suggestions:'Suggestions',acces:'Gestion des accès'};const SUPER_HASH='04445e6487736590d1ef50186b414e737e0164683cbbec64e00e73c000fd3bef';const $=s=>document.querySelector(s);const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().replace(/[^a-z0-9]+/g,'.').replace(/^\.|\.$/g,'');async function hash(t){const b=await crypto.subtle.digest('SHA-256',new TextEncoder().encode(t));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,'0')).join('')}function load(){let a=[];try{a=JSON.parse(localStorage.getItem(K)||'[]')}catch{}if(!a.some(u=>u.username==='superadmin'))a.unshift({id:'superadmin',firstName:'Super',lastName:'Admin',email:'',function:'Super Admin',role:'Super Admin',username:'superadmin',passwordHash:SUPER_HASH,firstLogin:false,active:true,permissions:ALL});localStorage.setItem(K,JSON.stringify(a));return a}function save(a){localStorage.setItem(K,JSON.stringify(a))}let users=load(),current=null;function loginUser(u){current=u;apply(u);login.style.display='none';app.style.display='block';go('home');renderAccess()}function defaults(fn){if(fn==='Président')return ALL;if(fn==='Secrétaire')return ['communication','sorties','adherents','suggestions'];if(fn==='Trésorier'||fn==='Trésorière')return ['adherents','comptabilite'];return ['suggestions']}function canCreate(){return current?.role==='Super Admin'}function canTerminate(){return current&&(current.role==='Super Admin'||current.function==='Président'||current.role==='Président')}function statusText(u){return !u.active?'Résilié':u.firstLogin?'Première connexion':'Actif'}function statusClass(u){return !u.active?'off':u.firstLogin?'first':'ok'}function initials(u){return ((u.firstName?.[0]||'')+(u.lastName?.[0]||'')).toUpperCase()||'U'}function checks(selected=[]){return `<div class="permGrid">${ALL.map(p=>`<label class="permCheck"><input type="checkbox" value="${p}" ${selected.includes(p)?'checked':''}><span><b>${LABELS[p]}</b><small>${p==='acces'?'Voir la gestion des utilisateurs':p==='comptabilite'?'Voir les paiements et rapports':'Accéder à cette rubrique'}</small></span></label>`).join('')}</div>`}function selected(box){return [...box.querySelectorAll('.permGrid input:checked')].map(x=>x.value)}function renderAccess(){const sec=$('#access');if(!sec)return;const list=users.filter(u=>u.id!=='superadmin'||current?.role==='Super Admin');sec.innerHTML=`<button class="back" data-go="home">← Retour</button><div class="page accessWrap"><div class="accessTop"><div><h1>Gestion des accès</h1><div class="sm">Utilisateurs, fonctions et droits individuels</div></div>${canCreate()?'<button class="primary" id="openCreate" type="button">＋ Ajouter un utilisateur</button>':''}</div>${canCreate()?`<div id="createBox" class="formCard" style="display:none"><div class="formGrid"><div><label>Prénom</label><input id="newFirst"></div><div><label>Nom</label><input id="newLast"></div><div class="full"><label>Adresse e-mail</label><input id="newMail" type="email"></div><div><label>Fonction dans l’association</label><select id="newFunction"><option>Président</option><option>Secrétaire</option><option>Trésorier</option><option>Trésorière</option><option>Membre du bureau</option><option>Autre</option></select></div><div><label>Identifiant généré</label><div id="generatedUser" class="generated">prenom.nom</div></div></div><div class="permTitle">Accès autorisés</div><div id="createPerms">${checks(defaults('Président'))}</div><div class="roleNote">La fonction sert d’information. <b>Les cases ci-dessus déterminent réellement ce que la personne verra dans l’application.</b></div><button id="createUserBtn" class="primary" type="button">Créer le compte</button></div>`:''}<div class="userList">${list.map(u=>`<div class="userCard2"><div class="avatar2">${initials(u)}</div><div class="meta2"><b>${u.firstName} ${u.lastName}</b><small>${u.function||u.role} • ${u.email||'Aucun e-mail'}</small><small>Identifiant : <b>${u.username}</b></small><small>Accès : ${(u.permissions||[]).map(p=>LABELS[p]).join(', ')||'Aucun'}</small><div class="userActions">${canCreate()&&u.id!=='superadmin'?`<button class="miniBtn" data-edit="${u.id}">Modifier les accès</button><button class="miniBtn" data-reset="${u.id}">Réinitialiser le mot de passe</button>`:''}${canTerminate()&&u.id!=='superadmin'?`<button class="miniBtn danger" data-term="${u.id}">${u.active?'Résilier l’accès':'Réactiver l’accès'}</button>`:''}</div></div><span class="pill ${statusClass(u)}">${statusText(u)}</span>${canCreate()&&u.id!=='superadmin'?`<div class="editPerms" id="edit-${u.id}" style="display:none"><div class="permTitle">Modifier les rubriques visibles</div>${checks(u.permissions||[])}<button class="primary savePerms" data-save="${u.id}" type="button">Enregistrer les accès</button></div>`:''}</div>`).join('')}</div></div>`;sec.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>go(b.dataset.go));$('#openCreate')?.addEventListener('click',()=>{$('#createBox').style.display=$('#createBox').style.display==='none'?'block':'none'});const sync=()=>{const v=[norm($('#newFirst')?.value),norm($('#newLast')?.value)].filter(Boolean).join('.');if($('#generatedUser'))$('#generatedUser').textContent=v||'prenom.nom'};$('#newFirst')?.addEventListener('input',sync);$('#newLast')?.addEventListener('input',sync);$('#newFunction')?.addEventListener('change',e=>{const box=$('#createPerms');box.innerHTML=checks(defaults(e.target.value))});$('#createUserBtn')?.addEventListener('click',()=>{const first=$('#newFirst').value.trim(),last=$('#newLast').value.trim(),email=$('#newMail').value.trim(),fn=$('#newFunction').value,username=[norm(first),norm(last)].filter(Boolean).join('.');if(!first||!last||!email||!username)return alert('Prénom, nom et adresse e-mail sont obligatoires.');if(users.some(u=>u.username===username))return alert('Cet identifiant existe déjà.');const u={id:crypto.randomUUID(),firstName:first,lastName:last,email,function:fn,role:fn==='Président'?'Président':'Compte',username,passwordHash:'',firstLogin:true,active:true,permissions:selected($('#createPerms'))};users.push(u);save(users);renderAccess();alert(`Compte créé.\nIdentifiant : ${username}\nLors de sa première connexion, la personne choisira son mot de passe.`)});sec.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>{const x=$('#edit-'+b.dataset.edit);x.style.display=x.style.display==='none'?'block':'none'});sec.querySelectorAll('[data-save]').forEach(b=>b.onclick=()=>{const u=users.find(x=>x.id===b.dataset.save);if(!u)return;u.permissions=selected($('#edit-'+u.id));save(users);renderAccess();alert('Les accès ont été mis à jour.')});sec.querySelectorAll('[data-term]').forEach(b=>b.onclick=()=>{const u=users.find(x=>x.id===b.dataset.term);if(!u)return;u.active=!u.active;save(users);renderAccess()});sec.querySelectorAll('[data-reset]').forEach(b=>b.onclick=()=>{const u=users.find(x=>x.id===b.dataset.reset);if(!u)return;u.passwordHash='';u.firstLogin=true;save(users);renderAccess();alert('Le compte repassera en première connexion.')})}function openFirstLogin(u){let box=$('#firstLoginBox');if(!box){box=document.createElement('div');box.id='firstLoginBox';box.className='firstLoginBox';box.innerHTML='<div class="firstLoginPanel"><h2>Première connexion</h2><p>Bonjour <b id="firstWho"></b>. Créez maintenant votre mot de passe personnel.</p><input id="p1" type="password" placeholder="Nouveau mot de passe"><input id="p2" type="password" placeholder="Confirmer le mot de passe"><button id="saveFirstPwd" class="primary">Créer mon mot de passe</button><div id="firstMsg" class="sm" style="margin-top:10px"></div></div>';document.body.appendChild(box)}$('#firstWho').textContent=`${u.firstName} ${u.lastName}`;$('#p1').value='';$('#p2').value='';box.classList.add('open');$('#saveFirstPwd').onclick=async()=>{const a=$('#p1').value,b=$('#p2').value;if(a.length<8)return $('#firstMsg').textContent='Le mot de passe doit contenir au moins 8 caractères.';if(a!==b)return $('#firstMsg').textContent='Les deux mots de passe ne correspondent pas.';u.passwordHash=await hash(a);u.firstLogin=false;save(users);box.classList.remove('open');loginUser(u)}}const form=$('#loginForm'),pass=$('#password');if(pass)pass.required=false;if(form){form.noValidate=true;form.onsubmit=async e=>{e.preventDefault();const un=$('#username').value.trim().toLowerCase(),pw=$('#password').value;users=load();const u=users.find(x=>x.username.toLowerCase()===un);if(!un){status.textContent='Saisissez votre identifiant.';return}if(!u){status.textContent='Identifiant inconnu.';return}if(!u.active){status.textContent='Ce compte a été résilié.';return}if(u.firstLogin||!u.passwordHash){status.textContent='';openFirstLogin(u);return}if(!pw){status.textContent='Saisissez votre mot de passe.';return}if(await hash(pw)!==u.passwordHash){status.textContent='Mot de passe incorrect.';return}status.textContent='';loginUser(u)}}$('#forgot')?.addEventListener('click',()=>status.textContent='La réinitialisation par e-mail sera branchée au serveur sécurisé.');const accessBtns=[...document.querySelectorAll('[data-permission="acces"], .dlist button')].filter(b=>b.textContent.includes('Gestion des accès'));accessBtns.forEach(b=>b.onclick=()=>{go('access');renderAccess()});})();