(()=>{
'use strict';
if(window.__sortiesTransportCarV4)return;window.__sortiesTransportCarV4=true;
const STORE='horticulture-sorties-safe-v2';
const $=(s,r=document)=>r.querySelector(s);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let createState={transportMode:'individual',coachDepartureTime:'',coachDepartureLocation:'',carpoolDepartureTime:'',carpoolMeetingLocation:''};
let editState={transportMode:'individual',coachDepartureTime:'',coachDepartureLocation:'',carpoolDepartureTime:'',carpoolMeetingLocation:''};
let lastEditId='';
function rows(){try{const a=JSON.parse(localStorage.getItem(STORE)||'[]');return Array.isArray(a)?a:[]}catch(_){return[]}}
function extra(s){try{return JSON.parse(String(s?.pricing||'{}'))||{}}catch(_){return{}}}
function currentSortie(){const title=$('#sortiesAdmin .sfs-detailMain h2')?.textContent?.trim()||'';return rows().find(x=>String(x.title||'').trim()===title)||null}
function style(){if($('#stcStyleV4'))return;const s=document.createElement('style');s.id='stcStyleV4';s.textContent=`.stc-box{border:1px solid #d6eadf;background:#f6fbf8;border-radius:14px;padding:13px;display:grid;gap:11px}.stc-head{display:flex;align-items:center;gap:9px;color:#07583f;font-weight:850}.stc-head svg{width:20px;height:20px;fill:none;stroke:currentColor;stroke-width:1.8;stroke-linecap:round;stroke-linejoin:round}.stc-fields{display:grid;grid-template-columns:.7fr 1.3fr;gap:9px}.stc-field{display:grid;gap:6px}.stc-field label{font-size:12px;font-weight:800}.stc-field input,.stc-field select{width:100%;border:1px solid #dce5df;border-radius:12px;padding:11px;background:#fff;font:inherit}.stc-note{font-size:11px;color:#62766d}.stc-hidden{display:none!important}@media(max-width:600px){.stc-fields{grid-template-columns:1fr}.stc-field input,.stc-field select{font-size:16px}}`;document.head.appendChild(s)}
const transportIcon='<svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 16h14l-1.5-5h-11zM7 16v2m10-2v2M4 13h16"/><path d="M8 8h8"/></svg>';
function normalizeState(st){const mode=['coach','carpool'].includes(st?.transportMode)?st.transportMode:'individual';return{transportMode:mode,coachDepartureTime:String(st?.coachDepartureTime||''),coachDepartureLocation:String(st?.coachDepartureLocation||''),carpoolDepartureTime:String(st?.carpoolDepartureTime||''),carpoolMeetingLocation:String(st?.carpoolMeetingLocation||'')}}
function loadEditState(){const s=currentSortie();if(!s)return;const p=extra(s);if(String(s.id)!==String(lastEditId)){lastEditId=String(s.id);editState=normalizeState(p)}}
function labelFor(st){if(st.transportMode==='coach')return`Car / bus organisé${st.coachDepartureTime?' · '+st.coachDepartureTime.replace(':',' h '):''}${st.coachDepartureLocation?' · '+st.coachDepartureLocation:''}`;if(st.transportMode==='carpool')return`Covoiturage organisé${st.carpoolDepartureTime?' · '+st.carpoolDepartureTime.replace(':',' h '):''}${st.carpoolMeetingLocation?' · '+st.carpoolMeetingLocation:''}`;return'Accès individuel'}
function renderFields(root,kind){const host=root.querySelector('.scw-section,.sew-section'),locationInput=root.querySelector('input[name="location"]');if(!host||!locationInput||root.querySelector('[data-stc-box]'))return;const st=kind==='create'?createState:editState;const box=document.createElement('div');box.className='stc-box';box.dataset.stcBox=kind;box.innerHTML=`<div class="stc-head">${transportIcon}<span>Transport jusqu’à la sortie</span></div><div class="stc-field"><label>Mode de transport</label><select data-stc-mode><option value="individual" ${st.transportMode==='individual'?'selected':''}>Accès individuel</option><option value="coach" ${st.transportMode==='coach'?'selected':''}>Transport en car / bus organisé</option><option value="carpool" ${st.transportMode==='carpool'?'selected':''}>Covoiturage organisé</option></select></div><div class="stc-fields ${st.transportMode==='coach'?'':'stc-hidden'}" data-stc-coach><div class="stc-field"><label>Heure de départ du car</label><input type="time" data-stc-time value="${esc(st.coachDepartureTime)}"></div><div class="stc-field"><label>Lieu de départ du car</label><input type="text" data-stc-location value="${esc(st.coachDepartureLocation)}" placeholder="Ex. Place de la Résistance, Châteaulin"></div></div><div class="stc-fields ${st.transportMode==='carpool'?'':'stc-hidden'}" data-stc-carpool><div class="stc-field"><label>Heure de rendez-vous</label><input type="time" data-stc-carpool-time value="${esc(st.carpoolDepartureTime)}"></div><div class="stc-field"><label>Lieu de rendez-vous du covoiturage</label><input type="text" data-stc-carpool-location value="${esc(st.carpoolMeetingLocation)}" placeholder="Ex. Parking de la mairie, Châteaulin"></div></div><div class="stc-note">L’IA préparera uniquement les indications pratiques adaptées au mode de transport choisi.</div>`;
 const locationField=locationInput.closest('.scw-field,.sew-field');
 const oldNote=root.querySelector('.scw-aiNote,.sew-note');
 if(locationField?.nextSibling)host.insertBefore(box,locationField.nextSibling);else host.appendChild(box);
 if(oldNote)oldNote.textContent='✨ L’IA complétera les informations d’accès à partir du lieu et du mode de transport que vous avez choisi.';
 const mode=$('[data-stc-mode]',box),coach=$('[data-stc-coach]',box),carpool=$('[data-stc-carpool]',box),time=$('[data-stc-time]',box),loc=$('[data-stc-location]',box),cpTime=$('[data-stc-carpool-time]',box),cpLoc=$('[data-stc-carpool-location]',box);
 const save=()=>{st.transportMode=['coach','carpool'].includes(mode.value)?mode.value:'individual';st.coachDepartureTime=time.value||'';st.coachDepartureLocation=loc.value.trim();st.carpoolDepartureTime=cpTime.value||'';st.carpoolMeetingLocation=cpLoc.value.trim();coach.classList.toggle('stc-hidden',st.transportMode!=='coach');carpool.classList.toggle('stc-hidden',st.transportMode!=='carpool')};
 mode.onchange=save;time.oninput=save;loc.oninput=save;cpTime.oninput=save;cpLoc.oninput=save;
}
function updateSummary(root,kind){const st=kind==='create'?createState:editState;const lines=[...root.querySelectorAll('.scw-line,.sew-line')];const target=lines.find(x=>/Accès\s*\/\s*transport/i.test(x.textContent||''));if(target)target.innerHTML=`<small>Accès / transport</small><b>${esc(labelFor(st))}</b>`}
function enhance(){style();document.querySelectorAll('[data-scw]').forEach(root=>{if(root.querySelector('input[name="location"]'))renderFields(root,'create');if(/Vérifier et créer/i.test(root.textContent||''))updateSummary(root,'create')});document.querySelectorAll('[data-sew]').forEach(root=>{if(root.querySelector('input[name="location"]')){loadEditState();renderFields(root,'edit')}if(/Vérifier les modifications/i.test(root.textContent||'')){loadEditState();updateSummary(root,'edit')}})}
function activeState(){if(document.querySelector('[data-sew]'))return editState;if(document.querySelector('[data-scw]'))return createState;return null}
function withTransport(sortie,st){if(!sortie||!st)return sortie;let p={};try{p=JSON.parse(String(sortie.pricing||'{}'))||{}}catch(_){p={}}p.transportMode=['coach','carpool'].includes(st.transportMode)?st.transportMode:'individual';p.coachDepartureTime=String(st.coachDepartureTime||'');p.coachDepartureLocation=String(st.coachDepartureLocation||'');p.carpoolDepartureTime=String(st.carpoolDepartureTime||'');p.carpoolMeetingLocation=String(st.carpoolMeetingLocation||'');if(p.transportMode==='coach'){const bits=[];if(p.coachDepartureTime)bits.push('départ à '+p.coachDepartureTime.replace(':',' h '));if(p.coachDepartureLocation)bits.push('rendez-vous : '+p.coachDepartureLocation);p.accessInfo='Bus : Départ du car'+(bits.length?' — '+bits.join(' · '):' organisé par l’association.')}else if(p.transportMode==='carpool'){const bits=[];if(p.carpoolDepartureTime)bits.push('rendez-vous à '+p.carpoolDepartureTime.replace(':',' h '));if(p.carpoolMeetingLocation)bits.push('lieu : '+p.carpoolMeetingLocation);p.accessInfo='Voiture : Covoiturage organisé'+(bits.length?' — '+bits.join(' · '):' par l’association.')}sortie.pricing=JSON.stringify(p);return sortie}
const nativeFetch=window.fetch.bind(window);
window.fetch=async function(input,init){try{if(init?.body&&typeof init.body==='string'){const body=JSON.parse(init.body),st=activeState();if(st&&(body.action==='saveSortieAdmin'||body.action==='createSortieHelloAsso')&&body.sortie){body.sortie=withTransport({...body.sortie},st);init={...init,body:JSON.stringify(body)}}}catch(_){}return nativeFetch(input,init)};
document.addEventListener('click',e=>{const bt=e.target.closest('[data-scw] [data-next], [data-sew] [data-next]');if(!bt)return;const root=bt.closest('[data-scw],[data-sew]'),box=root?.querySelector('[data-stc-box]');if(!box)return;const st=root.matches('[data-sew]')?editState:createState,mode=$('[data-stc-mode]',box),time=$('[data-stc-time]',box),loc=$('[data-stc-location]',box),cpTime=$('[data-stc-carpool-time]',box),cpLoc=$('[data-stc-carpool-location]',box);st.transportMode=['coach','carpool'].includes(mode.value)?mode.value:'individual';st.coachDepartureTime=time.value||'';st.coachDepartureLocation=loc.value.trim();st.carpoolDepartureTime=cpTime.value||'';st.carpoolMeetingLocation=cpLoc.value.trim();if(st.transportMode==='coach'&&(!st.coachDepartureTime||!st.coachDepartureLocation)){e.preventDefault();e.stopImmediatePropagation();alert('Indiquez l’heure et le lieu de départ du car.');return}if(st.transportMode==='carpool'&&(!st.carpoolDepartureTime||!st.carpoolMeetingLocation)){e.preventDefault();e.stopImmediatePropagation();alert('Indiquez l’heure et le lieu de rendez-vous du covoiturage.')}} ,true);
document.addEventListener('click',e=>{const bt=e.target.closest('#sortiesAdmin [data-new]');if(bt)createState={transportMode:'individual',coachDepartureTime:'',coachDepartureLocation:'',carpoolDepartureTime:'',carpoolMeetingLocation:''};const edit=e.target.closest('#sortiesAdmin button');if(edit&&/modifier/i.test((edit.textContent||'').trim())){lastEditId='';loadEditState()}},true);
const mo=new MutationObserver(()=>requestAnimationFrame(enhance));mo.observe(document.body,{childList:true,subtree:true});enhance();
})();